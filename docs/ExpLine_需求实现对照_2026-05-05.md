# ExpLine 需求实现对照 Checklist

## 总体状态

- [x] 已具备核心记录闭环：包装运行实验 -> 捕获 Git 变化 -> 读取项目说明和父实验 -> AI 生成语义记录 -> 保存实验节点 -> 更新索引。
- [x] 已具备基本跨分支父实验比较能力。
- [x] 已具备从实验事实记录重建 `index.json` 的基础能力。
- [x] 已具备命令行实验列表查看能力。
- [x] 已具备静态 HTML 可视化页面。
- [x] 已具备基于静态服务器和 VS Code Remote 端口转发的远程查看说明。
- [ ] 尚未实现 `view`、`link` 等辅助命令。

> 备注：需求文档文件名和标题已统一为 `v0.3`。

## 已实现

- [x] `expline init` 创建 `.expline/`、配置文件、Prompt 模板、项目摘要和索引文件。
- [x] `expline init --no-ai` 支持跳过 AI。
- [x] `expline rescan` 重新生成项目语义说明。
- [x] `expline rescan --no-ai` 支持本地回退摘要器。
- [x] `expline run -- <command>` 包装并执行实验命令。
- [x] 每次 `run` 生成一个新的实验目录和实验记录。
- [x] 实验记录保存 `command.txt`。
- [x] 实验记录保存 `diff.patch`。
- [x] 实验记录保存 `changed_files.txt`。
- [x] 实验记录保存 `record.json`。
- [x] 实验记录保存 AI 原稿 `record.ai.md`。
- [x] 实验记录保存用户可编辑版 `record.md`。
- [x] 实验记录保存 Git commit。
- [x] 实验记录保存 Git branch。
- [x] 实验记录保存工作区 dirty 状态。
- [x] 支持非 Git 项目降级运行。
- [x] 支持默认父实验推断：当前 commit 或祖先 commit 上最近实验。
- [x] 支持 `--parent EXP-xxxx` 手动指定父实验。
- [x] 指定或推断出的父实验不存在时，会在执行用户命令前报错。
- [x] 支持跨分支父实验比较：指定父实验时比较父实验 commit 到当前 commit 的 diff。
- [x] 支持 AI 生成结构化实验报告。
- [x] AI 调用失败或无 API Key 时回退到本地摘要器。
- [x] `expline show <experiment_id>` 查看实验记录。
- [x] `show` 默认读取用户可编辑的 `record.md`。
- [x] `expline edit <experiment_id>` 输出可编辑报告路径。
- [x] 初始化时生成项目摘要 Prompt 模板。
- [x] 初始化时生成实验记录 Prompt 模板。
- [x] 每次生成报告时保存渲染后的 prompt。
- [x] 每次生成报告时保存 AI 原始响应。
- [x] 支持 `--report-language <language>` 单次指定报告语言。
- [x] 支持 `expline config set report-language <language>` 持久化报告语言。
- [x] 支持 `OPENAI_BASE_URL` 环境变量。
- [x] 支持 `expline config set openai-base-url <url>`。
- [x] 支持 `--result-path` 记录轻量结果文件或目录摘要。
- [x] `--result-path` 只作为保存产物证据进入 Prompt，不用于解释指标变化原因或判断实验好坏。
- [x] 支持 Focused diff，优先把实验敏感代码和配置 diff 提供给 AI。
- [x] 支持 `expline config set diff-max-chars <n>`。
- [x] 支持 `expline config set focused-diff-max-chars <n>`。
- [x] 支持 `expline rebuild` 从 `.expline/experiments/*/record.json` 重建 `.expline/index.json`。
- [x] `rebuild` 会重建实验节点列表。
- [x] `rebuild` 会重建 commit 到实验节点的索引。
- [x] `rebuild` 会重新计算 `next_id`。
- [x] `rebuild` 会报告重复实验 ID、缺失父实验和损坏记录。
- [x] 支持 `expline list` 查看已有实验记录。
- [x] `list` 默认按创建时间倒序显示实验。
- [x] `list` 显示实验 ID、创建时间、父实验、Git 分支、Git commit 短哈希和标题。
- [x] `list` 支持 `--limit <n>` 限制显示数量。
- [x] `list` 支持 `--branch <branch>` 按 Git 分支过滤。
- [x] `list` 支持 `--parent <experiment_id>` 查看某个父实验的直接子实验。
- [x] `list` 会从 `record.json` 兼容补齐旧索引中缺失的分支等字段。
- [x] 支持 `expline site` 生成静态 HTML 实验谱系页面。
- [x] 静态页面生成到 `.expline/site/index.html`。
- [x] 静态页面为单文件 HTML，并内嵌实验链路和实验记录摘要。
- [x] `init` 后自动刷新静态页面。
- [x] `run` 后自动刷新静态页面。
- [x] `rebuild` 后自动刷新静态页面。
- [x] 静态页面支持节点点击查看实验详情。
- [x] 静态页面支持展示 `record.md` 内容。
- [x] 静态页面支持展示 `command.txt`、`changed_files.txt` 和 diff 片段。
- [x] 静态页面支持右键复制实验 ID。
- [x] 静态页面支持右键复制实验目录路径。
- [x] 静态页面支持右键复制 `record.md` 路径。
- [x] 静态页面支持右键复制 `diff.patch` 路径。
- [x] 静态页面支持右键复制 `command.txt` 路径。
- [x] 静态页面支持右键复制 VS Code 打开命令。
- [x] 静态页面支持搜索节点。
- [x] 静态页面支持高亮父链路和子链路。
- [x] README 已补充远程服务器通过 `python -m http.server --bind 127.0.0.1` 与 VS Code Remote 端口转发查看页面的说明。

## 部分完成或仍有风险

- [ ] `.expline/` 同步策略尚未明确区分工具仓库与用户实验项目。
  当前工具仓库 `.gitignore` 忽略 `.expline/`，但需求希望用户实验项目中的 `.expline/` 可随 Git 同步。
- [ ] `expline config get/set` 在未 init 的目录会创建 `.expline/`。
  这与“先 init 再配置”的直觉略有偏差。
- [ ] `run_user_command` 不捕获 stdout/stderr。
  当前符合“不保存完整日志”的边界，但未来可视化页面若需要运行摘要，会缺少日志来源。
- [ ] `diff.patch` 可能保存截断后的 diff。
  后续应区分“完整原始 diff 文件”和“提供给 AI 的截断 diff”。
- [ ] `record.json` 字段与需求示例不完全一致。
  当前实现使用 `evidence_index`，需求示例中写的是 `evidence_files`。
- [ ] `rescan` 会覆盖 `project_summary.md`。
  如果用户手动编辑过项目语义说明，可能造成修改丢失。
- [ ] `rebuild` 已具备基础重建能力，但还没有更完整的链路诊断报告。
  例如孤立节点分组、commit 索引差异报告、修复建议等还未实现。
- [ ] 静态 HTML 页面已可用，但布局仍是 MVP 级别。
  当前使用前端脚本按父子深度自动排布节点，适合中小规模实验；大量实验时后续可能需要更强的图布局、折叠、缩放和筛选能力。
- [ ] 静态页面中的 diff 仅内嵌预览片段。
  为避免单文件 HTML 过大，`diff.patch` 在页面中最多展示约 30000 字符；完整 diff 仍以实验目录中的 `diff.patch` 为准。

## 尚未实现

- [ ] `expline view`。
- [ ] `expline link --parent <parent_id> --child <child_id>`。
- [ ] 多分支 merge 后的推荐 Git 工作流文档。
- [ ] `.expline/index.json` 冲突后的标准恢复说明。

## 下一步优先级

- [x] 第一优先级：实现 `expline rebuild`。
- [x] 第二优先级：实现 `expline list`，用重建后的 `index.json` 快速查看实验列表。
- [x] 第三优先级：实现 `expline site`，生成最小静态 HTML 页面。
- [ ] 第四优先级：补充 Git 同步和多分支 merge 后的推荐使用流程。
- [x] 第五优先级：实现静态页面基础交互，包括搜索、点击详情、右键复制和链路高亮。
